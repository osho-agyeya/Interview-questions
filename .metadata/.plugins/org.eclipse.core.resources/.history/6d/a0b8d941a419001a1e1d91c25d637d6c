package ninja;

import java.util.Comparator;
import java.util.HashMap;
import java.util.TreeMap;


public class Get_Mode_Array_Updates {
	
	class Node{
		int key;
		int freq;
		Node(int key,int freq){
			this.key=key;
			this.freq=freq;
		}
	}
	
	public int[] getMode(int[] A, int[][] B) {
		int n=A.length;
		HashMap<Integer, Node > count= new HashMap<>();
		for(int i:A) {
			if(count.computeIfAbsent(key, mappingFunction))
		}
		
    }
}
